# Deploy website statis (folder public/) ke GitHub Pages
name: Deploy ke GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Upload folder public/ sebagai artifact Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: public

      - name: Deploy ke GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
