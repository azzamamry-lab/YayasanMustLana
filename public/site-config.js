/* ==========================================================================
   KONFIGURASI API BACKEND ADMIN — situs publik
   ==========================================================================
   Isi SITE_API_URL dengan alamat backend setelah di-deploy, contoh:
     window.SITE_API_URL = 'https://yayasan-api.onrender.com';
   Jika dibiarkan kosong (''), situs memakai backend di domain yang sama
   (cocok untuk pengembangan lokal: http://localhost:3002).
   ========================================================================== */
window.SITE_API_URL = '';
